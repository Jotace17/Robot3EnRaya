
#include <iostream>
#include <cmath>
#include <vector>

std::vector<float> Kinet_Dir(const float q1, const float q2, const float q3);
std::vector<float> Kinet_Inver(const float posx, const float posy, const float posz);
